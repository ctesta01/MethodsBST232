\documentclass[tikz, margin=3.14mm]{standalone}
\usepackage{pgfplots}

\usepackage{tikz}

\begin{document}
\begin{tikzpicture}

\begin{axis}[
    xlabel={x},
    ylabel={y},
    xmin=0, xmax=1,
    ymin=0, ymax=1,
    xtick={0,1},
    ytick={0,1},
    grid=both,
    grid style={line width=.1pt, draw=gray!10},
    major grid style={line width=.2pt,draw=gray!50},
    axis lines=middle,
    samples=100,
]

% Original wiggly non-negative function (like sin(x) convolved with a wide normal distribution)
% \addplot[blue, thick, domain=-10:10] {0.5*exp(-x^2/20)*(sin(deg(x)) + 1)};
% Normalized function (integral equals 1), dashed line
% \addplot[red, dashed, thick, domain=-10:10] {0.1*exp(-x^2/20)*(sin(deg(x)) + 1)};

\addplot[blue, thick, domain=0:1] {.25};

\node [circle, fill, inner sep = .1pt] (Y1) at (axis cs:.15, .09) {};

\end{axis}
\end{tikzpicture}
\end{document}